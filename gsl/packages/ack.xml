<?xml version="1.0"?>
<package name="ack">
  <message name="Acknowledgement" oneof_name="acknowledgement" oneof_tag="1">
    <description>Reply messages for actions that have no response data. Code `OK` means a positive acknowledgement, other codes
negative acknowledgements. A negative acknowledgement may be sent as the reply to any message, even if another reply
is expected. Negative acknowledgements may have a `message` describing the error condition.</description>

    <field name="code">
      <proto type="AcknowledgementCode" tag="1"/>
      <python type="int" default="OK"/>
    </field>
    <field name="message">
      <proto type="string" tag="2"/>
      <python type="str" default="''"/>
    </field>
  </message>
</package>
