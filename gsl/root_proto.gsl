.template 0
 ignorecase = 0

.endtemplate
.template 1
syntax = "proto3";

package $(string.replace(path, "/|.")).proto;

message $(message) {
    oneof payload {
.for protocol.package
        // $(name).proto
.  for message
        $(name) $(oneof_name) = $(oneof_tag);
.  endfor
.endfor
    }
}
.endtemplate
